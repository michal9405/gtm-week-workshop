.DS_Store
node_modules/
npm-debug.log*

# work/ holds outputs — keep them local, ship only the convention README
work/*
!work/README.md
# exception: the landing page project is shared work, not a local scratch output
!work/landing-page/

# Cowork/Claude personal project settings (machine-specific) — but DO ship .claude/skills/
.claude/settings.local.json

# stray tooling that leaked into the folder
.gh/

# GitHub CLI credential (never commit)
.gh/token
